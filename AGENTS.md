# codex_game 작업 지침

## 기준 작업공간

- 유일한 Git·코드·테스트·빌드 작업공간은 `C:\sk-encoa\codex_game`이다.
- 업무 문서의 기준 원본은 `G:\내 드라이브\codex_game\obsidian`이다.
- 다른 PC 설치·점검·복구 자료는 `G:\내 드라이브\codex_game\multi_pc`에 둔다.
- Git 저장소 안에 Obsidian Vault, Drive 동기화 폴더 또는 업무 문서 사본을 만들지 않는다.
- 여러 PC의 코드는 GitHub `dev`를 기준으로 동기화한다.
- G 최상위의 예전 `.git`은 이전 작업 보존본이며 Git 명령에 사용하지 않는다.

## 작업 시작 전

1. Drive의 `obsidian\rules`에서 공통 규칙과 자신의 역할 규칙을 확인한다.
2. `git -C C:\sk-encoa\codex_game status --short --branch`로 기존 변경을 확인한다.
3. 기존 변경은 사용자나 다른 작업자의 작업으로 간주하고 보존한다.
4. 작업 대상이 코드인지 문서인지 판단한다. 코드는 Git, 문서는 Drive에서 작업하는 것을 기본으로 한다.
5. 코드가 아닌 업무도 사용자가 Git 공유를 지정했거나 공동 검토·이력 관리·자동화에 필요한 경우 별도 목적의 Git 공유 대상으로 둘 수 있다.
6. 비코드 업무를 Git에 공유할 때는 Drive 기준 원본 여부를 명시하고, 같은 문서를 Drive와 Git에서 독립적으로 수정하지 않는다.
7. 다른 PC에서는 `multi_pc\SYNC_CHECK.ps1`로 Git과 Drive 상태를 함께 확인한다.

## 파일 위치

- 게임 코드와 코드에 필요한 에셋: `programer/`
- 테스트 코드: 게임 프로젝트 내부 테스트 디렉터리
- 빌드·배포 자동화: `.github/`와 `tools/`
- 기획·규칙·검토·QA·PM·제출 문서: `G:\내 드라이브\codex_game\obsidian`
- 저장소에 허용되는 문서: `README.md`, `AGENTS.md`, `CONTRIBUTING.md`와 코드 실행에 반드시 필요한 최소 안내
- 예외: 사용자가 명시적으로 Git 공유를 요청한 비코드 산출물. 기준 원본과 공유 목적을 기록하고 Drive 전체를 중복 적재하지 않는다.

## Git과 릴리스

- 일반 작업 기준 브랜치는 `dev`다.
- 사용자의 명시적 승인 없이 `pub`에 커밋, 푸시, 병합하거나 배포하지 않는다.
- 한 커밋에는 하나의 목적만 포함하며 다른 작업자의 변경을 함께 커밋하지 않는다.
- 강제 푸시, 강제 초기화와 이력 재작성은 합의 없이 수행하지 않는다.
- 데스크톱과 노트북에 같은 브랜치의 미푸시 변경을 동시에 남기지 않는다.
- 기기를 바꾸기 전에 코드는 commit·push, 문서는 Drive 동기화 완료를 확인한다.

## 빌드 버전과 리비전

- 공식 빌드는 선행 0이 없는 `Major.Minor.Patch` 형식을 사용한다.
- Major 증가 시 Minor·Patch를 0으로, Minor 증가 시 Patch를 0으로 초기화한다.
- 배포된 버전은 수정·덮어쓰기하지 않고, 변경분은 반드시 새 제품 버전으로 발행한다.
- 전체 Git SHA, SpecRevision과 ArtifactSHA256을 기록한다. 짧은 SHA는 표시용일 뿐 원본 식별자가 아니다.
- 리비전은 추적 정보이며 제품 버전 증가를 대신하지 않는다.
- `VERSIONING.md`와 `tools/New-VersionedBuild.ps1`를 구현 기준으로 삼는다.

## 제품 기준

- 공개 HTTPS 링크에서 설치·로그인·초대 없이 브라우저 플레이가 가능해야 한다.
- 첫 화면에서 목표, 조작법과 실행 방법을 확인할 수 있어야 한다.
- 첫 30초 이해와 3분 코어 재미를 우선한다.
- 외부 에셋 라이선스와 Codex 협업 기록은 Drive 문서 공간에 남긴다.

## 역할 경계

- 기획은 Drive에서 기획과 수용 기준을 관리하고 명시적 요청 없이 코드를 수정하지 않는다.
- 프로그래머는 Git에서 합의된 기획을 구현하고 QA 판정을 대신하지 않는다.
- QA는 Drive에 검증 결과를 기록하고 기획·코드·배포 설정을 직접 수정하지 않는다.
- PM·총괄은 Drive에서 일정·의존성·위험과 인계를 관리한다.
