# codex_game 작업 지침

## 기준 작업공간

- 유일한 Git·코드·테스트·빌드 작업공간은 `C:\codes\codex_game`이다.
- Obsidian 규칙과 내부 메모의 기준 원본은 `G:\내 드라이브\codex_game\obsidian`이다.
- C의 `obsidian` 디렉터리는 G의 Vault를 가리키는 로컬 연결이며 Git 추적 대상이 아니다.
- `G:\내 드라이브\codex_game` 최상위의 예전 `.git`, `.git\index.lock`, 인덱스와 중복 Git 파일을 수정·복구·커밋하지 않는다.
- 여러 PC 사이의 코드 동기화는 GitHub `dev`를 기준으로 하고, Drive의 `shared`에는 완료 커밋의 ZIP과 Git bundle만 발행한다.

## 작업 시작 전

1. `obsidian\rules\README.md`와 공통 협업 규칙을 읽는다.
2. 역할에 맞는 규칙을 읽는다.
   - 기획: `obsidian\rules\designer\기획_업무_규칙.md`
   - 프로그래머: `obsidian\rules\programer\프로그래머_업무_규칙.md`
   - QA: `obsidian\rules\QA\QA_업무_규칙.md`
   - PM·총괄: `obsidian\rules\PM\PM_업무_규칙.md`
3. `git -C C:\codes\codex_game status --short --branch`로 기존 변경을 확인한다.
4. 기존 변경은 사용자나 다른 작업자의 작업으로 간주하고 보존한다.

## 파일 위치

- 내부 검토·메모·증거: `obsidian\core` 아래 역할 폴더
- Git 공유 기획: `designer`
- 게임 코드와 기술 산출물: `programer`
- Git QA 산출물: `QA`
- Git PM·릴리스 산출물: `PM`
- 공통 Git 문서: `core`

동일한 문서를 C와 G의 예전 Git 사본에서 각각 수정하지 않는다. Git 공유 산출물의 기준은 항상 C 저장소다.

## Git과 릴리스

- 일반 작업 기준 브랜치는 `dev`다.
- 사용자의 명시적 승인 없이 `pub`에 커밋, 푸시, 병합하거나 배포하지 않는다.
- 한 커밋에는 하나의 목적만 포함하며 다른 작업자의 변경을 함께 커밋하지 않는다.
- 강제 푸시, 강제 초기화와 이력 재작성은 합의 없이 수행하지 않는다.
- 노트북 코딩도 로컬 Git clone에서 수행하며 commit·push가 가능하다. 기능 브랜치 푸시를 우선하고 Google Drive 안에 활성 `.git` 작업트리를 두지 않는다.
- 데스크톱과 노트북에 같은 브랜치의 미푸시 변경을 동시에 남기지 않는다.

## 제품 기준

- 공개 HTTPS 링크에서 설치·로그인·초대 없이 브라우저 플레이가 가능해야 한다.
- 첫 화면에서 목표, 조작법과 실행 방법을 확인할 수 있어야 한다.
- 첫 30초 이해와 3분 코어 재미를 우선한다.
- 외부 에셋 라이선스와 Codex 협업 기록을 남긴다.
- 현재 게임 코드와 엔진 프로젝트는 아직 없으며, 기획 P0 결정과 기술 스파이크가 구현 착수보다 먼저다.

## 역할 경계

- 기획은 명시적 요청 없이 코드·빌드·배포 설정을 수정하지 않는다.
- 프로그래머는 합의된 기획을 구현하고 QA 판정을 대신하지 않는다.
- QA는 기획·코드·에셋·기술 구조를 직접 수정하지 않는다.
- PM·총괄은 일정·의존성·위험과 인계를 관리하며 다른 역할의 최종 판단을 대신하지 않는다.
