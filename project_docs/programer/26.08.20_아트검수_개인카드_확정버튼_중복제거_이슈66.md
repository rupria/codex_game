# 아트 검수·프로그래머 인계 — 개인 카드 확정 버튼 중복 제거 (#66)

## 판정

신규 아트는 필요하지 않다. 최신 `dev`의 `PrivateSelection_0_5_5`에는 정상적인 확정 버튼 상태 3장이 이미 있으며, 문제 화면은 같은 기능을 담당하는 확정 조작이 두 번 그려진 표시·입력 중복이다.

## 유지할 최신 아트

`Assets/Art/Prototype/UI/PrivateSelection_0_5_5/`

- `private_selection_confirm_idle_180x52_0_5_5.png`
- `private_selection_confirm_active_180x52_0_5_5.png`
- `private_selection_confirm_disabled_180x52_0_5_5.png`

## 프로그래머 수정 요청

1. `PrivateSelectionDevPanel.DrawConfirm`의 `ConfirmRect (73, 418, 180, 52)`을 유일한 확정 버튼 렌더·입력 경로로 유지한다.
2. 화면 상단 또는 공통 하단 레이어에서 추가로 그리는 비동작 확정 버튼이 있으면 제거한다.
3. `PrivateCardsConfirmRequested` 호출은 유지한 단일 버튼에서 한 번만 발생해야 한다.
4. `Enter` 확정 단축키와 `snapshot.CanConfirm` 조건은 기존 규칙을 유지한다.
5. 중복을 가리기 위한 새 프레임이나 덮개 이미지를 추가하지 않는다.

## 확인 조건

- [ ] 확정 버튼이 화면에 한 개만 보인다.
- [ ] 보이는 한 개의 버튼이 마우스로 작동한다.
- [ ] 선택 수 부족 시 disabled, 충족 시 idle/active 상태가 보인다.
- [ ] 클릭 한 번당 확정 요청이 한 번만 발생한다.
- [ ] 키보드 확정과 카드 선택 규칙에 회귀가 없다.

기준 확인 SHA는 `f6584d0b41d9f0efc09d8a0b2dd361520276b9c1`이다. `pub`과 웹 배포는 변경하지 않는다.
