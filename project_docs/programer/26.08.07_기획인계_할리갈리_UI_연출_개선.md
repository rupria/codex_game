# 기획 인계 — 할리갈리 UI·연출 개선

- 작성일: 2026-08-07
- 요청 역할: 기획
- 인계 대상: 프로그래머
- 우선순위: 높음 — 첫 공개 플레이 빌드 전
- 상태: 0.1.0 구현 요청
- 현재 확정 기획: [게임진행플로우_0.1.0](../designer/게임진행플로우_0.1.0.md)
- 계승 규칙: `게임진행플로우_0.08`
- 개선 요청 원문: [할리갈리 화면 UI·연출 개선](../requests/2026-08-07_할리갈리_화면_UI_연출_개선_요청.md)

> 이번 인계는 0.1.0의 화면 구성, 조작성과 단계 전환 피드백 구현 요청이다. 0.08의 카드 판정, AI 확률, 승수, 제한 시간과 포커 규칙은 그대로 계승한다.

## 1. 함께 확인할 기획 문서

구현 판단은 다음 순서로 확인한다.

1. [기획 현재 기준](../designer/00_기획_현재기준.md)
2. [게임진행플로우 0.1.0](../designer/게임진행플로우_0.1.0.md)
3. [게임진행플로우 0.08](../designer/게임진행플로우_0.08.md)
4. [할리갈리 화면 UI·연출 개선 요청](../requests/2026-08-07_할리갈리_화면_UI_연출_개선_요청.md)
5. [프로토타입 아트소스 기획 기준](../designer/프로토타입_아트소스_기획기준.md)
6. [할리갈리 화면 아트·UI·연출 전달서](../../programer/CodexGame/ArtReferences/Halli_0_06/halli_screen_art_motion_handoff_0_06.md)

- 1~2번은 현재 확정 기획이다.
- 3번은 0.1.0이 계승하는 코어 규칙 기준이다.
- 4번은 0.1.0에 통합된 개선 요청의 원문이다.
- 기존 문서와 UI 표현이 충돌하면 0.1.0의 중앙 벨·시작 화면·획득 카드·순차 공개·단계 전환 항목을 우선 적용하되 계승 게임 규칙은 0.08을 유지한다.
- Drive 기준 원본은 `G:\내 드라이브\codex_game\obsidian\core\designer`이며 Git 문서는 역할 간 공유용이다.

## 2. 구현 목표

처음 보는 플레이어가 설명 없이 다음 네 가지를 화면에서 이해하게 한다.

1. 지금 할리갈리 단계가 진행 중이다.
2. 중앙 카드를 펼친 뒤 두 벨 중 하나를 선택한다.
3. 판정 결과로 어떤 카드를 누가 획득했는지 확인한다.
4. 할리갈리가 끝나면 포커 단계로 전환된다.

## 3. 필수 구현 범위

### A. 최초 실행 화면

- 공개 시작 화면의 조작 버튼은 `START`와 `GUIDE`만 남긴다.
- 게임 로고·제목과 배경 장식은 표시할 수 있다.
- 개발 빌드명, 에셋 로드 상태, 긴 목표·조작 설명은 제거한다.
- `GUIDE`는 별도 세션을 시작하지 않는 오버레이로 구현한다.
- 가이드에는 `카드 펼치기 → 같은 문양 해골 합 3 판정 → 벨 선택 → 획득 카드로 포커 승패 예측`을 이미지 중심으로 표시한다.
- `START`를 누르기 전에는 게임 타이머를 시작하지 않는다.

### B. 첫 커뮤니티 카드 공개

- `START` 후 딜러가 첫 커뮤니티 카드 한 장을 펼치는 연출을 재생한다.
- 카드를 잠시 확대하고 상단 중앙의 첫 커뮤니티 카드 슬롯에 고정한다.
- 옆에는 잠긴 두 번째 커뮤니티 카드 슬롯을 항상 표시한다.
- 공개 연출 중에는 펼치기와 벨 입력을 잠근다.
- 할리갈리 30초 입력 제한은 조작 가능 상태가 된 뒤 시작하거나, 연출 시간을 제한 시간에서 제외한다.
- 시작부터 할리갈리 조작 가능 상태까지 3초 이내를 목표로 한다.

### C. 할리갈리 화면 배치

- 기준 해상도는 `960×540`, 화면은 3/4 탑다운 구도다.
- 플레이어는 청록색, AI는 붉은색으로 구분한다.
- 상단 중앙: 첫 커뮤니티 카드와 잠긴 두 번째 카드
- 좌측 중앙: 왼쪽 필드 카드
- 우측 중앙: 오른쪽 필드 카드
- 중앙: 왼쪽 청록 벨과 오른쪽 붉은 벨
- 두 벨 아래 중앙: 카드 펼치기 버튼 또는 카드 더미
- 상단 좌우: 플레이어·AI HP와 할리갈리 승수
- 하단 좌우: 플레이어 획득 카드·아이템과 AI 획득 상태

### D. 마우스·키보드 입력

- 벨을 테이블 양 끝에 두지 않고 중앙에 나란히 배치한다.
- 왼쪽 벨은 왼쪽 필드, 오른쪽 벨은 오른쪽 필드 판정이다.
- 왼쪽 벨 권장 중심은 `X 44%, Y 58%`, 오른쪽 벨은 `X 56%, Y 58%`다.
- 두 벨 사이에는 화면 너비의 최소 `6%` 이상 빈 공간을 두고 클릭 영역은 보이는 벨 크기의 `115~125%`로 설정한다.
- 카드 펼치기 클릭 영역은 두 벨 아래에 두고 벨 클릭 영역과 겹치지 않게 한다.
- 마우스와 키보드를 동시에 지원한다.
- 키보드 기준은 `Q = 왼쪽 벨`, `W = 카드 펼치기`, `E = 오른쪽 벨`이다.
- 현재 구현의 방향키·스페이스 입력은 보조 입력으로 유지할 수 있지만 화면 가이드의 기본 표기는 `Q/W/E`로 통일한다.
- 입력 잠금 상태에서는 버튼의 비활성 표현과 실제 입력 차단이 일치해야 한다.

### E. 카드 한 장씩 순차 공개

- 양측 카드를 동시에 나타내지 않고 카드 한 장마다 이동·회전 연출을 완료한 뒤 다음 카드를 공개한다.
- `W` 또는 마우스 펼치기 입력 1회에 실제 카드 4장을 소비하고 다음 순서로 처리한다.

```text
플레이어 왼쪽 → AI 왼쪽 → 플레이어 오른쪽 → AI 오른쪽
→ 네 번째 CardRevealComplete
→ 벨 조건 판정과 AI 반응 시작
```

- 카드 1장은 중앙 더미에서 슬롯까지 `0.18~0.22초` 동안 이동·앞면 전환한다.
- 한 카드가 완전히 보인 뒤 `0.06~0.10초` 간격을 두고 다음 카드를 공개하며 전체는 `0.9~1.2초` 안에 완료한다.
- 순차 공개 중에는 펼치기와 벨 입력을 잠그며 중복 입력으로 카드 소비 순서가 바뀌지 않아야 한다.
- 사용자 요청의 마지막 `AI 본인 기준 왼쪽`은 좌우 교대와 대칭 구성을 위해 `AI 본인 기준 오른쪽`으로 적용한다.
- 직전 승자가 다음 배분을 플레이어 입력 대기 또는 AI 자동 진행으로 시작하지만 네 카드 슬롯 공개 순서는 유지한다.
- 한 번의 배분을 카드 펼치기 1회로 계산하고 최대 12회 또는 카드 4장 미만에서 단계를 종료한다.
- 0.08의 25회·2장 공개, 선행 카드 우선 공개와 입력 시점 AI 반응 규칙은 이 항목으로 대체한다.

### F. 획득 카드 표시

- 플레이어 획득 후보 카드는 좌측 하단 트레이에 앞면으로 계속 표시한다.
- 획득 순간에는 필드 카드가 플레이어 트레이로 이동하는 `0.35~0.45초` 연출을 사용한다.
- 획득 연출은 0.08의 판정 후 리뷰 시간 안에서 처리하며 다음 입력 가능 시점을 임의로 늦추지 않는다.
- AI 획득 카드는 판정 직후 앞면을 잠시 보여준 뒤 우측 하단에 뒷면과 장수로 표시한다.
- AI의 전체 후보 앞면은 지속적으로 공개하지 않는다.
- 단순한 마지막 획득 텍스트만으로 완료 처리하지 않는다.

### G. 할리갈리에서 포커로 전환

- 할리갈리 종료 시 벨과 필드 입력을 잠근다.
- 필드와 벨 UI를 정리하면서 테이블을 와이드 구도로 줌아웃한다.
- 획득 후보 카드를 포커 선택·손패 영역으로 재배치한다.
- 필드·벨 채도 저하 `0.2~0.3초`, 카드 재배치 `0.4~0.6초`, 포커 줌아웃 `0.55~0.7초`를 기준으로 한다.
- 전환 완료 후 다음 게임 상태로 한 번만 진행한다. 중복 클릭이나 중복 `Advance`가 발생하지 않아야 한다.
- 할리갈리 입력 UI는 포커 단계에서 보이거나 클릭되지 않아야 한다.
- 자유 카메라 대신 `할리갈리 클로즈업`과 `포커 와이드` 두 프리셋만 사용한다.

### H. AI 영역

- AI 측에 플레이어와 같은 조작 버튼을 표시하지 않는다.
- `AI 판단 중`, 반응 상태와 획득 카드 수를 표시하는 상태 영역으로 사용한다.
- AI의 실제 반응 시각이나 정답 벨을 미리 노출하지 않는다.

## 4. 화면 상태와 입력 허용표

| 화면 상태 | 표시 | 허용 입력 |
|---|---|---|
| 시작 화면 | 로고, `START`, `GUIDE` | 시작, 가이드 열기 |
| 가이드 | 핵심 행동 안내 | 닫기 |
| 첫 카드 공개 | 딜러·커뮤니티 카드 연출 | 입력 잠금 |
| 할리갈리 준비 | 필드, 중앙 벨, 펼치기 | `W`·마우스 펼치기, 유효 상태의 `Q/E`·마우스 벨 |
| 카드 순차 공개 | 고정 순서의 카드 4장 | 입력 잠금, 네 번째 카드 공개 후 벨 판정 |
| 벨 판정·획득 리뷰 | 결과와 카드 이동 | 다음 판정에 영향을 주는 입력 잠금 |
| 할리갈리 종료 전환 | 정리·줌아웃 | 입력 잠금 |
| 개인 카드·포커 | 획득 카드, 공용 카드, 포커 UI | 해당 단계 입력만 허용 |

## 5. 현재 구현에서 확인할 지점

다음은 구현 위치를 강제하는 요구가 아니라 현재 `dev` 구조에서의 확인 지점이다.

- `PlayableDevView.DrawIntro()` — `START`·`GUIDE` 시작 화면
- `PlayableDevView.HandleHalliInput()` — `Q/W/E`와 기존 보조 키 입력
- `HalliDevPanel.Draw()`·`DrawActions()` — 필드, 중앙 벨과 펼치기 배치
- `PrototypeHalliSession`의 현재 2장 `SequentialReveal` — 고정 순서 4단계 공개, 네 번째 완료 후 판정과 AI 반응 시작으로 전환
- `HalliTurnOrder` — 다음 배분의 플레이어 입력 대기·AI 자동 시작만 결정하고 네 카드 슬롯 순서는 변경하지 않도록 역할 분리
- `HalliStageRules` — 기존 최대 25회·카드 2장 미만 종료를 최대 12회·카드 4장 미만 종료로 전환
- `PrototypeHalliSnapshot` — 현재 획득 장수와 마지막 획득 카드만 제공하므로 지속 트레이에 필요한 누적 카드 읽기 모델 확인
- 순차 배분 화면 상태 — 현재 공개 카드 번호, 대상 슬롯과 마지막 `CardRevealComplete`를 프레젠테이션이 식별할 수 있는 읽기 모델 확인
- `PlayableGamePhase` 전환 — 첫 카드 공개와 할리갈리 종료 연출을 게임 규칙 상태와 분리할지 검토
- `PlayableDevView.OnGUI()` — 현재 기준 좌표가 `960×600` 스케일과 `960×540` 보드를 함께 사용하므로 기준 화면을 `960×540` 안전 영역으로 통일할지 확인

0.1.0은 카드 판정식은 유지하지만 펼치기 1회의 소비량, 최대 횟수, 입력 잠금과 AI 반응 시작 시점을 변경한다. 프레젠테이션 애니메이션과 애플리케이션 상태가 같은 `CardRevealComplete`를 기준으로 진행되어야 하며, 연출 중 게임 타이머가 감소하거나 입력이 세션에 전달되면 안 된다.

## 6. 권장 작업 순서

1. 시작 화면을 `START`·`GUIDE` 구조로 정리한다.
2. `960×540` 기준 할리갈리 고정 배치를 만든다.
3. 중앙 벨 2개와 펼치기 버튼의 마우스·`Q/W/E` 입력을 연결한다.
4. 입력 1회의 4장 순차 배분, 최대 12회와 입력 잠금을 연결한다.
5. 누적 획득 카드 읽기 모델과 플레이어·AI 트레이를 연결한다.
6. 첫 커뮤니티 카드 공개와 할리갈리 클로즈업을 연결한다.
7. 할리갈리 종료 후 포커 와이드 전환을 연결한다.
8. 화면 크기, 입력 잠금, 타이머와 중복 진행을 회귀 검증한다.

## 7. 완료 조건

- [ ] 최초 실행 화면에서 조작 버튼은 `START`와 `GUIDE`만 보인다.
- [ ] 가이드에서 게임 목표와 `Q/W/E` 조작을 확인할 수 있다.
- [ ] 첫 커뮤니티 카드 공개 중 입력이 들어가지 않고 할리갈리 제한 시간이 손해 보지 않는다.
- [ ] 좌우 필드, 중앙 벨 2개와 펼치기 버튼을 처음 보는 사람이 즉시 구분한다.
- [ ] 마우스·`Q/W/E` 조작 결과가 동일하다.
- [ ] 클릭 영역이 겹치지 않고 비활성 상태에서 입력이 처리되지 않는다.
- [ ] 펼치기 입력 1회에 실제 카드 4장을 소비하고 `플레이어 왼쪽 → AI 왼쪽 → 플레이어 오른쪽 → AI 오른쪽` 순서로 한 장씩 보인다.
- [ ] 네 번째 카드 공개 뒤에만 벨 조건과 AI 반응을 시작한다.
- [ ] 4장 배분은 최대 12회이며 카드 4장 미만이면 기존 무승자 종료 흐름으로 진행한다.
- [ ] 플레이어 획득 카드가 전용 트레이에 누적 표시된다.
- [ ] AI 획득 상태가 조작 버튼으로 오해되지 않는다.
- [ ] 할리갈리 종료 후 한 번만 포커 단계로 진행한다.
- [ ] 포커 단계에서 할리갈리 입력이 처리되지 않는다.
- [ ] 정답 벨과 AI 반응 시점을 입력 전에 노출하지 않는다.
- [ ] `960×540`, `1280×720`, `1920×1080`에서 핵심 UI가 안전 영역을 벗어나지 않는다.
- [ ] 0.1.0이 변경하지 않은 0.08 카드 판정·AI 확률·승수·보상·포커 회귀 테스트가 통과한다.
- [ ] 2장·25회·입력 시점 AI 반응을 가정한 기존 테스트는 4장·12회·네 번째 카드 이후 반응 기준으로 갱신된다.

## 8. 구현 후 인계 요청

프로그래머는 완료 시 다음을 공유한다.

- 적용 커밋 전체 SHA와 사용한 기획 문서 버전
- `START → 첫 커뮤니티 카드 → 할리갈리 → 획득 카드 → 포커 진입` 영상 또는 연속 캡처
- `960×540`, `1280×720`, `1920×1080` 화면 확인 결과
- 마우스와 `Q/W/E` 입력 확인 결과
- 0.08 회귀 테스트 결과
- 미적용 항목, 임시 에셋과 알려진 제한

## 9. 제외 범위

- 0.08의 동일 문양·해골 3 카드 판정, AI 결과 확률, `3→2→2`, 30초·3분·1분 제한 시간과 포커 규칙 변경
- 0.1.0에서 명시하지 않은 추가 밸런스 변경
- 최종 캐릭터 일러스트·고급 FX 확정
- 자유 카메라·회전 카메라
- 정답 자동 강조와 합계 표시
- 프로그래머가 기획 미확정 규칙을 임의로 확정하는 작업
