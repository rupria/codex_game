# 이슈 #26 더미 바 상점·스테이지 이동 구현 결과

- 작업 브랜치: `codex/halli-ui-issues-21-25`
- 기준 이슈: <https://github.com/rupria/codex_game/issues/26>
- 상태: 상점·전환 로직과 최종 아트 연결 완료, 개발 WebGL 검증·배포 진행
- `dev`·`pub`: 직접 변경하지 않음

## 현재 구현 범위

- `StageWon → BarShop → NextStageTransition → 다음 스테이지` 런타임 경계를 분리했다.
- 스테이지 보상은 상점 진입 전에 기존 잔여 HP 기준으로 한 번만 정산한다.
- 더미 상품 데이터 6종에 `id`, 현지화 이름 Key, 아이콘 Key, 가격, 효과 Key, 표시 상태를 둔다.
- 방문 시 시드 기반으로 3개 슬롯을 구성하고, 무료 리롤 1회로 나머지 3개 상품을 전부 교체한다.
- 같은 방문의 두 번째 리롤과 `다음 가게로` 중복 입력은 상태 게이트에서 거부한다.
- 다음 상점 방문에서는 리롤 상태를 초기화한다.
- 상점 화면에는 정산된 총알, 현재 HP, 상품 3개, 리롤과 다음 가게 버튼만 표시한다.
- 구매 버튼은 비활성이고 실제 구매·총알 차감·인벤토리·아이템 효과를 호출하지 않는다.
- 상점과 다음 스테이지 전환 중에는 할리갈리·포커 입력 및 전투 타이머를 진행하지 않는다.

## 아트 연결 경계

최종 아트 SHA `bf2ebd9137b9d24c8cfdf330d165daba259b7b8f`의 전달물을 `BarShopUiArtSet`에 주입했다. 아트 미연결 시에는 절차형 폴백 화면이 표시되며 런타임 로직은 동일하다.

- 배경: `Background` — 960×540
- 상품 슬롯 공통 프레임: `SlotFrame`
- 리롤 버튼: `RerollIdle`, `RerollHover`, `RerollPressed`, `RerollDisabled`
- 다음 가게 버튼: `ContinueIdle`, `ContinueHover`, `ContinuePressed`
- 상태 패널: `BulletPanel`, `HealthPanel`
- 상품 아이콘 바인딩: `bar_shop.item.dummy_01`~`bar_shop.item.dummy_06`

문자열은 이미지에 굽지 않고 런타임 현지화 Key로 출력한다. 최종 인계 좌표에 맞춰 상태 패널, 상품 슬롯, 리롤·다음 진행 버튼의 표시 및 클릭 영역을 연결했다.

## 현지화 추가

- 런타임 CSV를 146 Key로 확장했다.
- 추가 Key: 리롤 2개, 구매 1개, 진열 상품 이름 6개.
- 공개 화면에는 `미확정`, `개발 중`, 플레이스홀더 같은 내부 안내 문구를 표시하지 않는다.

## 스테이지 이동 0.3.1 연결

- 아트 기준: PR #27 최종 인계 `bf534fa588b92cca6f422c5b9867705cd69f2dc4`
- `상점 UI 정리 → 출구 회전 → 걷기 → 스윙도어 개방 → 문턱 통과 → 암전`을 1.90초 고정 순서로 재생한다.
- 닫힌·열린 배경, 좌우 문 4상태, 먼지 4프레임, 비네트, 페이드, 해골 로딩 8프레임을 `StageTransitionUiArtSet`으로 분리 주입한다.
- 실제 로드 완료 신호가 1.90초보다 늦으면 암전과 입력 잠금을 유지한다. 암전 0.15초 이후에는 0.09초 간격 해골 로딩 링을 반복한다.
- 로드 완료 뒤 0.35초 페이드인이 끝난 시점에만 전환 토큰을 한 번 소비하고 다음 스테이지를 시작한다.
- 현재 단일 씬 개발 빌드는 직렬화된 아트 준비 완료를 실제 로드 완료 신호로 사용한다. 향후 비동기 씬 로더는 같은 신호 경계만 교체하면 된다.

## 검증

- Core smoke tests: `All CodexGame core smoke tests passed.`
- 전환 경계 테스트: 1.90초 각 상태 경계, 0.15초 로딩 표시 지연, 빠른·느린 로드, 0.35초 페이드인, 중복 소비 방지
- Unity `6000.3.18f1`: 신규 이미지 24개 import 및 C# 어셈블리 컴파일 성공
- WebGL 전체 검증·개발 배포 SHA와 URL은 배포 완료 후 본 문서와 이슈 #26에 추가한다.

## 범위 밖

- 실제 아이템 종류·효과
- 구매 처리, 가격 확정, 총알 차감, 인벤토리
- 체력 회복 가격·회복량
- 유료·다회 리롤
- 최종 상점·전환 사운드
- 최종 3D 문 오브젝트 회전(현재는 교체 가능한 2D 4상태 Sprite 방식)
- `pub` 병합과 배포
