# 게임진행플로우 0.06 아키텍처 기반 구현 상태

- 작성일: 2026-08-07
- 담당 역할: 프로그래머
- 문서 상태: 기반·F01·F02 구현 완료, F04 구현 진행 중
- 기준 명세: `게임진행플로우_0.06`
- 기준 브랜치: `dev`
- 명세 공유 커밋: `223595d3dc3ce57bc085603222b605b951cc34bb`
- 기반 구현 커밋: `d7ebb862c1183a7c0c078f360da926908203b2ff`
- F01·F02 구현 커밋: `fb966e9d0e6118e34089face8776a59644cb3bf8`
- F04 작업 시작 기준 커밋: `b4da2948cbe14b19ed9fe9004d242175c5e59bbf`
- Drive 기준 원본: `G:\내 드라이브\codex_game\obsidian\core\programer\26.08.07_게임진행플로우_0.06_아키텍처_기반_구현상태.md`
- Git 공유본: `project_docs/programer/26.08.07_게임진행플로우_0.06_아키텍처_기반_구현상태.md`

## 1. 현재 결론

기능별 병렬 개발을 위한 아키텍처 기반, 0.06 카드 획득 판정, F01 카드·덱·난수와 F02 더미·카드 보존은 `origin/dev`에 공유됐다.

현재 상태는 순수 C# 테스트와 Unity 컴파일이 가능한 개발 기반이다. 플레이 씬과 전체 게임 흐름은 아직 없으므로 실행 가능한 게임 빌드 상태는 아니다.

## 2. 구현 완료 범위

### 레이어 경계

- `CodexGame.Core`: Unity 비의존 결정적 규칙
- `CodexGame.Application`: Core를 사용하는 명령·이벤트·상태 머신·Port 셀
- `CodexGame.Presentation`: Unity 입력·View·Animation·Audio·Art 셀
- `CodexGame.Infrastructure`: 저장·온라인·시간 구현 셀
- `CodexGame.Bootstrap`: 런타임 조립 셀

레이어 간 참조 방향은 asmdef로 제한했다.

### 기능 셀

- `Core/Shared`
- `Core/Cards`
- `Core/Halli`
- `Core/Poker`
- `Core/Ai`
- `Core/Battle`
- `Core/Rewards`
- `Application/Commands`
- `Application/Events`
- `Application/StateMachine`
- `Application/Ports`
- `Application/Snapshots`
- `Presentation/Input`
- `Presentation/Views`
- `Presentation/Animation`
- `Presentation/Audio`
- `Infrastructure/Persistence`
- `Infrastructure/Online`
- `Infrastructure/Time`

Core를 기능별 asmdef로 과도하게 나누지 않고 하나의 Core 어셈블리 안에서 폴더와 namespace로 병렬 작업 경계를 만들었다.

### 0.06 판정

- 판정 입력에 카드 문양과 해골 수를 함께 전달한다.
- 같은 문양의 `1+2`, `2+1`만 양쪽 카드를 획득한다.
- 서로 다른 문양의 `1+2`, `2+1`은 합이 3이어도 획득하지 않는다.
- 해골 3 카드는 상대 카드 문양과 관계없이 해골 3 카드만 획득한다.
- 미확정 `3+3`은 임의 결정하지 않고 `Unspecified`로 유지한다.

### F01·F02 카드 기반

- 표준 52장 카드 ID·문양·랭크 모델
- 미확정 해골 분포를 `ICardSkullPolicy` 뒤에 격리
- 전투 라운드 시드 기반 결정적 셔플과 난수 채널 분리
- 첫 공용 카드 1장 사용 후 51장 유지
- 모든 카드 위치를 추적하는 52장 `CardLedger`
- 좌우 더미 최대 2장, 세 번째 카드 적용 시 가장 오래된 카드의 미획득 이동
- 선택한 더미만 초기화하고 반대 더미 상태 유지
- 실제 `Card` 모델을 0.06 획득 판정에 연결

## 3. 검증 결과

- 순수 C# smoke test: 통과
- 동일 문양 `1+2`, `2+1`: 통과
- 상이 문양 `1+2`, `2+1`: 통과
- 해골 3 예외와 단독 해골 3: 통과
- 잘못된 해골 수 입력 거부: 통과
- 표준 52장 유일성·동일 시드 덱 재현·난수 채널 분리: 통과
- 더미 최대 2장·가장 오래된 카드 이동·반대 더미 유지: 통과
- 전체 카드 영역 52장 보존: 통과
- .NET 경고 0개·오류 0개
- Unity `6000.3.18f1` 배치 컴파일: return code 0
- 최종 Git 상태: `dev`와 `origin/dev` 일치

## 4. 실행 빌드와 시험 URL

- 실행 가능한 Unity 씬: 없음
- `EditorBuildSettings` 등록 씬: 0개
- WebGL 실행 빌드: 미생성
- Cloudflare 시험 URL: 미생성
- `pub`: 변경 없음

플레이 씬이 없으므로 컴파일 성공을 실행 빌드 완료로 보고하지 않는다. 공개 시험 URL은 플레이 가능한 WebGL 산출물과 공개 배포 조건이 준비된 뒤 별도 승인 범위에서 생성한다.

## 5. 남은 미구현 범위

- F04 벨 33.3ms 경계와 정수 마이크로초 시간 모델
- F05 할리갈리 단계 종료·승수·타이머
- F06 개인 카드 배분
- F07 포커 평가
- F08 1스테이지 AI
- F09 전투·HP·보상·스테이지 전환
- F10 Application 상태 머신
- F11 플레이 씬과 Unity 입력·표현 연결
- F12 로컬 세이브·온라인 결과 기록

## 6. 명세 확인 대기 항목

- `3+3` 획득 선택 규칙
- A 스트레이트 인정 범위
- 아이템 피해 변경 규칙
- 예측 성공 코인 증가량과 아이템 목록
- 2스테이지 이후 AI 정책
- 최종 에셋 출처·라이선스·공개 빌드 승인

미확정 항목은 임의 확정하지 않고 정책 경계 뒤에 둔다.

## 7. 현재 진행 중 작업

F04 벨·시간 판정을 구현 중이다.

1. 정수 마이크로초 타임스탬프 값 객체
2. `33_299us`, `33_300us`, `33_301us` 경계 판정
3. 다음 카드 펼치기 시 이전 벨 기회와 AI 예약 무효화 계약

각 기능은 순수 C# 테스트를 통과한 뒤 별도 목적의 커밋으로 공유한다.

## 8. 동시 작업 기준

- 작업 시작 전 `origin/dev` fetch와 로컬 기준 일치를 확인한다.
- 공용 타입과 Application 상태 머신은 담당자를 지정하고 동시에 수정하지 않는다.
- 다른 파트의 변경을 발견하면 임의 정리하지 않고 보존한다.
- 완료 후 브랜치, 전체 커밋 SHA, 구현·미구현 범위, 테스트·빌드·URL 상태를 함께 공유한다.
