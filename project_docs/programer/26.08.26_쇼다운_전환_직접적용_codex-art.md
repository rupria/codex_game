# 쇼다운 전환 직접 적용 — codex/art

## 적용 기준

- 승인 영상 `three_call_to_showdown_fullflow_v02_10s.mp4`의 10초 호흡을 전환 기준으로 사용한다.
- Three Call 종료 후 `GameRules.ThreeCallToSelectionPresentationMicroseconds`에 정의된 10초 동안 입력을 잠근다.
- 0~1초에는 테이블을 유지하면서 중앙으로 포커스를 모은다.
- 첫 번째 커뮤니티 카드는 0.8~1.7초, 두 번째 커뮤니티 카드는 1.6~2.4초에 순서대로 이동·플립한다.
- 2초부터 SHOWDOWN 표찰을 노출하고, 두 카드가 모두 공개된 뒤 남은 시간 동안 완성 상태를 유지한다.
- 개인 카드, 획득 카드 트레이, 조작 문구와 활성 벨 입력 UI는 전환 화면에 노출하지 않는다.
- 승인 영상 후반부의 개인 카드 3장·선택 프레임은 최신 확정에서 제외됐으므로 런타임에 재현하지 않는다.

## 런타임 변경

- `PlayableDevView`에서 `HalliTransition`을 일반 Three Call 렌더 경로와 분리했다.
- 전환 전용 화면을 먼저 그린 후 즉시 반환하므로 기존 카드 더미·획득 UI가 뒤에서 섞여 나오지 않는다.
- `Presentation0124Panel.DrawThreeCallToSelection`에는 전체 개인 카드 선택 스냅샷 대신 두 커뮤니티 카드 값만 전달한다.
- 카드 앞·뒷면 및 플립 모션은 유지하되, 중복되던 상·하단 레일 프레임과 청록·붉은 포커스 곡선은 사용하지 않는다.
- 좌우 Three Call 카드, 벨, 하단 카드 뒷면은 전환 화면에서 제거한다.

## 비변경 범위

- 카드 판정과 선택 규칙
- 개인 카드 선택 기능과 팝업
- 사운드 에셋과 사운드 연결
- 저장 장면과 Unity `.meta`

## 검수

- 전체 코어 스모크 테스트 통과
- `PresentationFlow0124Tests`의 2장 준비, 10초 입력 잠금, 개인 카드 미노출 유지 조건 통과

## 승인 기준 원본

- `programer/CodexGame/ArtReferences/ShowdownTransition_0_1_0/reference/three_call_to_showdown_fullflow_v02_10s.mp4`
- 해상도: 1280×720 / 30fps / 10.0초
- 원본 영상은 구도와 타이밍 기준이며, 카드 값은 런타임 데이터를 사용한다.

## 총괄 전달

- 브랜치: `codex/art`
- 최종 SHA: `a4be7068d35eccabfa90d5ae11268111f1732190`
- 기대 결과: Three Call 종료 후 10초 쇼다운 전환이 실행되고 커뮤니티 카드 2장만 순차 공개되며 개인 카드는 노출되지 않는다.
- 수정 금지 범위: 사운드 에셋·사운드 연결, Unity 장면·`.meta`, 카드 판정, 개인 카드 선택 규칙과 팝업
