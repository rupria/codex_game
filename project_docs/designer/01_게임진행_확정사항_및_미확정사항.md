---
문서 상태: 진행 중
문서 성격: 확정 규칙 명세 및 미확정 사항 단일 관리 문서
기준 명세: 게임진행플로우_0.1.2.2.md
최종 수정: 2026-08-10
담당: 기획
---

# 게임진행 확정사항 및 미확정사항

## 1. 문서 목적

이 문서는 `게임진행플로우_0.1.0.md` 이후 대화에서 확정된 규칙과 아직 결정되지 않은 질문을 한곳에서 관리한다.

- `게임진행플로우_0.1.2.2`에서 변경된 재화·아이템·AI·전환·UI 규칙이 이 문서보다 우선한다.
- 아래 `확정사항`은 0.1.2.2에서 변경하지 않은 주제에 한해 같은 주제의 구버전 문구보다 우선한다.
- 아래 `미확정 사항`은 구현자가 임의로 해석하지 않는다.
- 사용자는 각 질문의 `답변:` 뒤에 내용을 작성한다.
- 답변이 확정되면 해당 항목을 확정사항으로 옮기고 결정 이력에 기록한다.
- 이 문서는 계속 갱신하는 관리 문서이므로 미확정 질문마다 새 파일을 만들지 않는다.

관련 문서:

- [[00_기획_현재기준]]
- [[게임진행플로우_0.1.2.2]]
- [[게임진행플로우_0.1.2.2_작업요청]]
- [[07_인게임_텍스트_최소화_및_재화_UI_기준]]
- [[게임진행플로우_0.1.0]] — 계승 원본
- [[밸런싱/해골_카드_분포_밸런싱_0.1.0.1]]
- [[03_가이드_및_카드_UI_개선_기준]]
- [[04_스테이지_보상_바_상점_및_전환_기준]]

## 2. 우선순위와 적용 범위

1. 이 문서에서 `확정`으로 표시된 최신 결정
2. `게임진행플로우_0.1.0.md`
3. 0.08 이하의 보존 문서

이 문서는 카드 확률과 해골 카드 배정표를 직접 변경하지 않는다. 확률·분포는 밸런싱 문서에서 관리하고, 여기서는 카드 판정·획득·저장소·포커 전환 규칙을 관리한다.

---

# 확정사항

## D-001. 할리갈리 기본 정답 조건

- 상태: 확정
- 판정 문양은 `클로버(♣)`, `하트(♥)`, `다이아몬드(♦)`, `스페이드(♠)` 4종이다.
- 기본 정답은 **같은 문양이면서 해골 수 합계가 3인 카드 조합**이다.
- 해골 `1 + 2` 또는 `2 + 1` 조합은 두 카드의 문양이 같을 때 정답이다.
- 해골 3 카드의 단독 획득 규칙은 D-003을 따른다.
- 각 벨은 자신에게 대응하는 왼쪽 또는 오른쪽 카드 더미 한쪽만 판정한다.
- 한쪽 카드 더미에서 동시에 판정하는 카드는 최대 2장이며, 반대편 카드와 합산하지 않는다.
- 따라서 `1 + 1 + 1`은 정답으로 인정하지 않는다.

## D-002. 해골 1·2 정답 카드 획득

- 상태: 확정
- 같은 문양의 해골 1 카드와 해골 2 카드로 정답이 성립하면 두 장을 모두 획득한다.
- 두 카드는 정답을 맞힌 참가자의 개인 획득 카드 저장소로 이동한다.
- 획득된 두 카드는 미획득 카드풀과 이후 재셔플 대상에서 제외한다.

```text
같은 문양 해골 1 + 해골 2
→ 유효 벨 판정
→ 먼저 정답 입력한 참가자 결정
→ 해골 1 카드와 해골 2 카드 모두 획득
→ 개인 획득 카드 저장소로 이동
```

## D-003. 해골 3 정답 카드 획득

- 상태: 확정
- 해골 3 조건으로 성공하면 펼치기 단계에 노출된 해골 3 카드 한 장만 획득한다.
- 같은 카드 더미에 다른 해골 카드가 함께 있어도 해골 3 카드만 개인 획득 카드 저장소로 이동한다.
- 함께 있던 비선택 카드는 획득 카드로 처리하지 않는다.
- 해골 3 카드가 여러 장 노출됐을 때 어느 한 장을 선택하는지는 기존 최신 명세의 `가장 최근에 펼쳐진 해골 3 카드` 규칙을 유지한다.

## D-004. 양쪽 조건 동시 성립과 입력 우선순위

- 상태: 확정
- 왼쪽과 오른쪽이 모두 정답 조건을 만족하면 먼저 누른 쪽의 벨만 정답으로 처리한다.
- 먼저 선택된 유효 카드 더미만 획득 처리한다.
- 왼쪽 벨과 오른쪽 벨은 각각 대응하는 카드 더미만 판정하며, 화면 전체의 같은 문양 카드를 합산하지 않는다.
- 입력 시각이 최신 명세의 동시 판정 범위에 들어오면 플레이어를 우선한다.
- 최초 판정이 확정된 뒤 들어온 같은 프레임의 추가 입력과 연타는 무시한다.

## D-005. 카드 공개 중 벨 입력과 카드 상태

- 상태: 확정
- 직전 승패가 확정된 직후 다음 진행에 **2초 펼치기 잠금**을 1회 적용한다.
- 2초 잠금 중에는 벨 입력을 받지 않으며, 잠금이 끝난 뒤에는 카드가 순차 공개되는 도중에도 벨 입력을 받을 수 있다.
- 벨을 누른 순간의 카드 상태를 기준으로 선택한 카드 더미의 정답 여부를 판정한다.
- 카드는 앞면 전환이 완료된 시점부터 벨 판정 대상이 된다.
- 선택 카드가 앞면 전환을 완료했다면 획득 가능 카드에 포함한다.
- 선택하지 않은 카드 더미의 최상단 카드는 획득하지 않고 해당 더미 맨 아래로 이동한다.

> 이 결정은 `게임진행플로우_0.1.0.md`의 `4장 배분 중 벨 입력 잠금`, `네 번째 CardRevealComplete 이후 판정 시작` 규칙을 변경한다.

## D-006. 카드 펼치기 순서

- 상태: 확정
- 배분 시작 참가자와 관계없이 다음 순서를 유지한다.

```text
1. 플레이어 본인 기준 왼쪽 카드 공개
2. AI 본인 기준 왼쪽 카드 공개
3. 플레이어 본인 기준 오른쪽 카드 공개
4. AI 본인 기준 오른쪽 카드 공개
```

- AI의 네 번째 카드는 `AI 본인 기준 오른쪽`으로 확정한다.
- 공개 중 벨로 라운드 결과가 확정되면 남은 카드 공개를 즉시 중단하고 다음 할리갈리 라운드로 이동한다.
- 카드의 제출 주체가 보이도록 플레이어 카드 펼치기 저장소는 화면 하단 중앙, AI 카드 펼치기 저장소는 화면 상단 중앙에 둔다.
- 플레이어 카드는 하단 덱에서 판정 슬롯 방향으로 위쪽 이동하고, AI 카드는 상단 덱에서 판정 슬롯 방향으로 아래쪽 이동한 뒤 앞면을 공개한다.
- 위·아래 출발점과 이동 방향은 대면 연출을 위한 표시 규칙이며, 왼쪽·오른쪽 벨의 논리 슬롯과 판정 범위는 변경하지 않는다.
- 실제 게임 화면에서는 검토 이미지의 흰색 여백 대신 살룬·원형 테이블 배경이 이어져야 한다.
- 세부 이동·회전·도착 수용 기준은 [[03_가이드_및_카드_UI_개선_기준]]의 `대면형 덱 배치와 카드 제출 연출`을 따른다.

## D-007. 획득 카드 저장과 포커 사용

- 상태: 확정
- 할리갈리 단계에서 획득한 카드는 참가자별 개인 획득 카드 저장소에 보관한다.
- 저장된 획득 카드는 포커 단계의 개인 카드 구성에 사용한다.
- 포커 판정에 실제 사용하는 개인 카드는 정확히 3장이다.
- 개인 획득 카드가 3장을 초과하면 플레이어와 AI 모두 포커에 사용할 3장을 직접 선택한다.
- 기존 `3 → 2 → 2` 직접 선택 규칙을 유지한다.
- 할리갈리 단계 패자도 정상적으로 획득한 카드를 유지하며, 3장을 초과하면 동일하게 직접 선택한다.
- 오답 또는 제한 시간 초과로 해당 할리갈리 라운드에서 패배해도, 이전 라운드에서 정상 획득한 카드는 빼앗기거나 상대의 보상 카드 선택 대상으로 이동하지 않는다.
- 개인 획득 카드가 3장 미만이면 할리갈리 단계 종료 시 미획득 카드풀에서 시드 기반 무작위로 지급해 정확히 3장을 구성한다.
- 카드 펼치기에 사용하려 했으나 소진되지 않은 카드도 단계 종료 시 미획득 카드풀에 포함한다.
- 개인 획득 카드는 현재 전투의 포커 라운드까지 유지한다.
- 포커 판정과 해당 전투 라운드가 끝나면 개인 획득 카드 저장소를 초기화하며, 다음 전투 라운드는 새 52장 덱과 새 개인 카드 구성으로 시작한다.

## D-008. 카드 부족과 미획득 카드풀 처리

- 상태: 확정
- 카드 펼치기 저장소가 4장 미만이어도 남은 카드가 있으면 순차 공개를 계속한다.
- 전투 라운드의 할리갈리 단계 진행 중에는 미획득 카드풀을 재셔플해 펼치기 저장소를 다시 채우지 않는다.
- 다음 카드 공개가 필요하지만 펼치기에 사용할 카드가 없으면 할리갈리 단계를 즉시 종료한다.
- 현재 필드에 노출됐지만 획득되지 않은 카드와 펼치기에 사용하지 못한 잔여 카드는 단계 종료 시 미획득 카드풀에 포함한다.
- 이 종료 시점에 개인 카드가 3장 미만이면 D-007의 무작위 보충 규칙을 적용한다.
- 새 덱 구성과 재셔플은 포커 라운드가 끝난 뒤 다음 전투 라운드를 시작할 때 새 시드로 수행한다.

> `게임진행플로우_0.1.0.md`의 `전투 라운드 진행 중 재셔플하지 않는다` 규칙을 유지한다.

## D-009. 제한 시간·오조작·동시 입력

- 상태: 확정
- 기본 제한 시간은 30초다.
- 제한 시간 초과는 해당 할리갈리 라운드의 패배 1회로 판정한다.
- 오조작 또는 오답 벨은 해당 할리갈리 라운드의 패배 1회로 판정한다.
- 제한 시간 초과·오조작·오답으로 승리한 상대에게는 승리 1회만 기록한다.
- 위 사유로 패배한 참가자의 기존 획득 카드를 빼앗지 않으며, 상대에게 별도의 보상 카드 선택 단계도 제공하지 않는다.
- 플레이어와 AI 입력이 동시로 판정되면 플레이어를 우선한다.
- 직전 승패 확정 직후 2초 펼치기 잠금을 1회 적용하고, 잠금이 끝나면 30초 벨 입력 제한 타이머를 시작한다.
- 2초 잠금이 끝난 뒤에는 카드 공개 도중에도 벨 입력할 수 있으며, 앞면 전환이 완료된 카드만 판정에 포함한다.

## D-010. AI 대전

- 상태: 확정
- 기본 게임 모드는 플레이어 대 AI 대전이다.
- AI도 플레이어와 같은 카드 판정, 획득, 개인 카드 3장과 포커 비교 규칙을 사용한다.
- AI의 반응 시간과 정답·오답·놓침 확률은 별도 밸런싱 값으로 관리한다.

## D-011. 포커 사용 카드

- 상태: 확정
- 플레이어와 AI는 각각 개인 카드 3장을 사용한다.
- 양측은 커뮤니티 카드 2장을 공유한다.
- 포커 족보 판정에는 개인 카드 3장과 커뮤니티 카드 2장, 정확히 5장을 모두 사용한다.
- 일부 카드만 골라 사용하는 베스트 핸드 방식은 사용하지 않는다.

## D-012. 포커 동률 처리

- 상태: 확정
- 동률 처리는 최신 명세의 포커 비교 규칙을 사용한다.
- 랭크 비교가 모두 같으면 같은 비교 위치의 카드 문양을 비교한다.
- 문양 우선순위는 `♠ 스페이드 > ♦ 다이아몬드 > ♥ 하트 > ♣ 클로버`다.
- 랭크와 문양 비교를 끝까지 적용하므로 최종 동률은 발생하지 않는다.

## D-013. 승패 예측 시점

- 상태: 확정
- 모든 아이템 사용과 카드 변경을 마친 뒤, 실제 승패를 공개·확인하기 직전에 승패 예측을 진행한다.
- 예측 입력을 확정한 뒤 AI 개인 카드 공개와 포커 결과 판정을 진행한다.
- 예측 결과는 포커 승패 자체를 변경하지 않고 별도 보상 판정에만 사용한다.
- 승패 예측 제한 시간은 2분이다.

## D-014. 아이템 사용과 승패 예측

- 상태: 확정
- 아이템은 승패 예측 단계에 진입하기 전에만 사용할 수 있다.
- 기본 5장 승부에는 아이템 사용 개수 제한을 두지 않으며 보유한 사용 가능 아이템을 연속으로 사용할 수 있다.
- 아이템 사용 개수 제한은 향후 특정 스테이지의 패널티 규칙으로만 적용할 수 있으며 기본 규칙에는 적용하지 않는다.
- 예측 성공 횟수는 현재 스테이지에서 집계하며 최대 5회다.
- 예측 실패·2분 무반응은 성공 횟수를 늘리지 않고 일회성 재화를 지급하지 않는다.
- 스테이지 승리 시 예측 성공 결과에 따른 일회성 재화를 별도 지급한다.
- 아이템으로 카드가 변경되면 변경된 최종 카드 상태를 확인한 뒤 승패 예측을 한 번 진행한다.
- 카드 변경 시 재예측하지 않으며 예측 제한 시간도 초기화하지 않는다.
- 예측 성공 횟수별 일회성 재화는 `0·1·1·2·2·3개`로 지급한다. 실패·무반응은 성공 횟수를 늘리지 않으며 기존 성공 누적은 유지한다.

## D-015. 할리갈리 최대 진행 횟수와 조기 종료

- 상태: 확정
- 할리갈리 단계는 최대 12회까지 진행하며, 1회당 최대 4장을 순차 배분한다.
- 12회 한도에 도달하면 할리갈리 단계를 종료한다.
- 한도에 도달하기 전이라도 펼치기에 사용할 카드가 없으면 즉시 종료한다.
- 종료 시 플레이어 또는 AI의 개인 카드가 3장 미만이면 미획득 카드풀에서 시드 기반 무작위 카드를 지급해 정확히 3장을 구성한다.

## D-016. 스테이지 승리 보상과 재화

- 상태: 확정
- AI의 HP가 0이 되는 순간 플레이어의 스테이지 승리를 확정한다.
- 스테이지 클리어 보상은 기본 재화, 예측 성공 보상은 일회성 재화로 분리한다.
- 스테이지 클리어 기본 재화는 1·2·3스테이지 모두 2개를 지급한다.
- 잔여 HP 보너스는 적용하지 않는다. 계산 과정에서 소수점이 발생하면 버림 처리한다.
- 예측 성공 0~5회에 따른 일회성 재화는 각각 0·1·1·2·2·3개를 지급한다.
- 기본 재화는 현재 런 동안 다음 스테이지로 이월한다.
- 일회성 재화는 승리 직후 상점에서 먼저 사용하고 상점 이탈 시 남은 수량을 소멸한다.
- 캐릭터 사망 시 기본·일회성 재화와 현재 런 진행을 초기화한다.
- 일반 플레이 화면에는 재화 명칭을 표시하지 않고 서로 다른 아이콘과 숫자만 표시한다.
- 1차 아이템 3종, 가격, 인벤토리·구매 제한과 체력 회복 가격은 `게임진행플로우_0.1.2.2`를 따른다.
- 공개 화면에 `미확정`, `개발 중` 같은 내부 문구를 노출하지 않는다.

## D-017. 게임 시작과 다음 스테이지 전환

- 상태: 확정
- 게임 최초 시작 시 `가게로 걸어감 → 살룬 스윙도어를 열고 입장 → 실내 중앙 원형 테이블로 접근 → 착석 → 상대 소개 및 전투 시작` 순서로 진행한다.
- 스테이지 승리 뒤에는 `기본·일회성 재화 분리 정산 → 현재 가게의 바 → 상점 이탈 → 전환 연출 → 새 가게 입장 → 새 상대 소개 → 중앙 원형 테이블 착석 → 다음 전투 시작` 순서로 진행한다.
- 상점 이탈 후 전환 연출 재생 중에는 모든 게임 입력을 잠그고, 실제 연출 재생 완료 이벤트에서 다음 스테이지를 한 번 시작한다.
- 전환 시간은 별도 고정 초 수가 아니라 실제 연출 재생시간을 기준으로 한다. 전환 연출 건너뛰기는 허용하지 않는다.

## D-018. 회복 아이템

- 상태: 확정
- 가격은 재화 1개이며 사용 시 HP 1을 회복한다.
- 최대 HP는 3이고 최대 HP를 초과해 회복할 수 없다.
- 최대 HP 상태에서도 1개를 구매·보유할 수 있지만 사용할 수 없다.

## D-019. 3스테이지 클리어와 런 완료

- 상태: 확정
- 3스테이지 완료 후 최종 상점에 진입하지 않고 클리어 연출과 결과 화면을 재생한다.
- 결과 화면에는 예측 성공 횟수, 최종 승리·패배 결과, 해당 런에서 완성한 가장 높은 족보를 표시한다.
- 결과 확인 후 남은 재화와 아이템을 초기화하고 메인 화면으로 이동한다.

## D-020. 상점 리롤

- 상태: 확정
- 리롤 비용은 1이고 상점 방문당 최대 2회 사용할 수 있다.
- 상점 방문이 바뀌면 사용 횟수를 초기화한다.

## D-021. 플레이 구간 상태 표시

- 상태: 확정
- `할리갈리 단계`의 인게임 명칭은 `Three Call`, `포커 단계`의 인게임 명칭은 `Showdown`으로 사용한다.
- 일반 플레이 화면에는 기존 `할리갈리`, `포커` 문자열을 표시하지 않는다.
- 구간 진입 시 아이콘과 `Three Call 돌입` 또는 `Showdown 돌입` 라벨 메시지를 표시하고, 세부 상태는 아이콘과 카메라 구도 변화로 전달한다.
- 내부 문서·코드 용어는 기존 명칭을 유지하고 표시 계층에서 신규 명칭을 공통 사용한다.

## D-022. 스테이지 아이템 사용 제한 패널티

- 상태: 확정
- 1스테이지에는 적용하지 않는다.
- 2·3스테이지 입장 시 같은 진행에서 패널티가 아직 발동하지 않았다면 스테이지마다 한 번 20% 확률로 발동 여부를 결정한다.
- 발동 시 해당 스테이지 전체 아이템 총사용 한도를 1개 또는 2개 중 무작위로 하나 선정하고 입장 연출과 함께 공개한다.
- 아이템 사용 횟수는 해당 스테이지의 모든 Showdown에서 누적하며, 한도 소진 후에는 스테이지 종료 전까지 추가 사용할 수 없다.
- 같은 진행에서 한 번 발동한 뒤에는 이후 스테이지에서 다시 추첨하거나 발동하지 않는다. 2스테이지 미발동 시에만 3스테이지에서 다시 추첨한다.

---

# 기존 0.1.0과 달라진 규칙

| 주제 | 게임진행플로우 0.1.0 | 이 문서의 최신 결정 | 적용 |
|---|---|---|---|
| 공개 중 벨 입력 | 4장 배분 중 잠금, 네 번째 공개 후 판정 | 승패 직후 2초 잠금 1회 이후 순차 공개 중 입력 가능 | D-005 우선 |
| 덱 재셔플 | 전투 라운드 중 재셔플 금지 | 진행 중 재셔플 금지 유지, 카드 소진 시 단계 종료 | D-008 유지·명확화 |
| 공개 순서 | 플레이어 좌 → AI 좌 → 플레이어 우 → AI 우 | 동일 | 유지 |
| 해골 1·2 획득 | 유효 더미 두 장 획득 | 해골 1·2 두 장 모두 획득 | 유지·명확화 |
| 해골 3 획득 | 해골 3 카드 한 장 획득 | 펼치기 단계의 해골 3 카드만 획득 | 유지·명확화 |
| 포커 카드 수 | 개인 3장 + 공용 2장 | 정확히 5장 모두 사용 | 유지·명확화 |
| 포커 동률 | 문양 우선순위로 최종 결정 | 최신 명세 규칙 사용 | 유지 |
| 아이템과 예측 | 아이템 후 패 확정, 이후 예측 | 아이템은 예측 전에만 사용, 최종 카드로 2분 예측 1회 | D-013~D-014 우선 |
| 아이템 제한 패널티 | 후순위 검토 | 2·3스테이지 20% 판정, 스테이지 총사용 한도 1~2개, 한 진행 1회만 발동 | D-022·0.1.2.4 우선 |
| 할리갈리 진행 한도 | 최대 12회 4장 배분 | 최대 12회 유지, 카드 소진 시 조기 종료·3장 미달 무작위 보충 | D-015 우선 |
| 오답·시간 초과 보상 | 상대 보상 카드 선택 상태 존재 | 상대 승리 1회만 기록, 기존 획득 카드 유지·별도 보상 선택 없음 | D-007·D-009 우선 |
| 스테이지 보상 | 코인 보유 수치만 표시 | 기본·일회성 재화를 분리하고 정확한 지급량은 Q-015~Q-016 답변으로 확정 | 0.1.2.3·Q-015~Q-016 우선 |
| 스테이지 사이 진행 | 별도 바·가게 전환 없음 | 총알 정산 → 바 → 가게 이동·입장 → 상대 소개 → 중앙 원형 테이블 착석 | D-017 우선 |

---

# 미확정 사항

전투 코어 규칙의 Q-001~Q-014 답변은 모두 확정사항 D-001~D-015에 반영했다. 아래 Q-015 이후 항목은 `게임진행플로우_0.1.2.4`를 보면서 사용자가 직접 답변해 확정한다.

## 답변 작성 방법

- 각 항목의 코드 블록 안에서 `답변:` 뒤를 작성한다.
- 선택지에 없는 규칙도 자유롭게 추가할 수 있다.
- 한 항목만 먼저 답변해도 되며, 답변이 확정되면 확정사항과 결정 이력으로 옮긴다.
- 조커 실질 등장률, `3→2→2` 템포, AI 체감처럼 플레이 데이터가 필요한 항목은 이 답변란에서 즉시 확정하지 않고 밸런스 검증 결과를 먼저 사용한다.

## Q-015. 스테이지 클리어 기본 재화 지급량

- 상태: 확정
- 현재 0.1.2.4 기준: 1·2·3스테이지 클리어 시 기본 재화 2개를 지급하고 잔여 HP 보너스는 적용하지 않는다.
- 충돌 정리: 기존 D-016의 `잔여 플레이어 HP 1당 1개`는 폐기하고 스테이지별 기본 2개 지급을 우선한다.
- 질문: 1·2·3스테이지 지급량, 잔여 HP 보너스, 소수점 처리 기준을 어떻게 정하는가?
- 영향: 상점 구매량, 런 전체 재화 누적, 회복 아이템 가치

```text
답변:
- 1스테이지 기본 지급량: 2
- 2스테이지 기본 지급량: 2
- 3스테이지 기본 지급량: 2
- 잔여 HP 보너스 적용 여부와 계산식: 잔여 HP 보너스 미적용 진행
- 소수점 처리 기준: 버림 처리
```

## Q-016. 예측 성공 일회성 재화 지급량

- 상태: 확정
- 현재 0.1.2.4 기준: 예측 성공 보상은 직후 상점에서 기본 재화보다 먼저 소비되고 상점 이탈 시 소멸하는 일회성 재화다.
- 질문: 현재 스테이지의 예측 성공 0~5회에 각각 몇 개를 지급하는가?
- 영향: 예측 동기, 상점 소비량, 기본 재화 보존 속도

```text
답변:
- 0회 성공: 0
- 1회 성공: 1
- 2회 성공: 1
- 3회 성공: 2
- 4회 성공: 2
- 5회 성공: 3
- 실패·무반응 시 기존 성공 누적 유지 여부: 실패.무반응시 기존 성공 누적 유지
```

## Q-017. 체력 회복 아이템의 회복량과 최대 HP

- 상태: 확정
- 현재 0.1.2.4 기준: 가격은 재화 1개, 회복량은 HP 1, 최대 HP는 3이다. 최대 HP 상태에서도 1개를 구매·보유할 수 있지만 사용할 수 없다.
- 질문: 회복량, 최대 HP, 최대 HP 상태의 구매·사용 처리를 어떻게 정하는가?
- 영향: 상점 선택, 생존 난이도, 재화 가치

```text
답변:
- 1회 회복량: HP 1 회복
- 최대 HP: 3
- 최대 HP 초과 회복 허용 여부: 회복 불가
- 최대 HP 상태 구매 가능 여부: 1개 구매가능
- 최대 HP 상태 사용 가능 여부: 사용 불가
```

## Q-018. 패 확정 단계 제한 시간과 무입력 처리

- 상태: 확정
- 현재 0.1.2.4 기준: 아이템 사용과 패 확정은 2분 제한 시간을 공유한다. 시간 초과 시 해당 Showdown 자동 패배와 HP 1 피해를 적용하고 승패 예측을 생략한다.
- 질문: 패 확정 제한 시간, 시간 초과 자동 처리, 아이템 사용 중 타이머 처리를 어떻게 정하는가?
- 영향: 무한 대기 방지, 아이템 연속 사용, 포커 단계 템포

```text
답변:
- 패 확정 제한 시간: 패 확정 제한 시간 2분
- 시간 초과 시 자동 처리: 시간 초과 시 포기, 승패 예측 미적용 처리
- 패 확정 시간 초과의 포기는 해당 Showdown 자동 패배와 HP 1 피해로 처리하며 승패 예측은 진행하지 않는다.
- 아이템 사용 중 타이머 처리: 아이템 사용 중 패 확정 시간과 공유
```

## Q-019. 3스테이지 클리어 후 최종 상점과 런 완료

- 상태: 확정
- 현재 0.1.2.4 기준: 3스테이지 완료 후 최종 상점 없이 클리어 연출과 결과 화면을 거쳐 재화·아이템을 초기화하고 메인 화면으로 이동한다.
- 질문: 최종 상점, 남은 재화·아이템, 결과 화면, 이후 이동 경로를 어떻게 처리하는가?
- 영향: 최종 보상의 의미, 런 종료 경험, 재도전 흐름

```text
답변:
- 최종 상점 진입 여부: 3스테이지 완료 후 클리어 연출 및 메인화면 복귀
- 최종 상점에서 재화·아이템 사용 목적: 남은 재화, 아이템 초기화
- 결과 화면 표시 항목: 예측 성공 횟수, 승리, 패배시 가장 높은 족보
- 결과 화면 이후 이동 경로: 메인화면 이동
```

## Q-020. 아이템 사용 제한 스테이지 패널티

- 상태: 확정
- 현재 0.1.2.4 기준: 기본 아이템 사용 개수 제한은 없고, 확률로 발동한 스테이지 패널티만 스테이지 전체 총사용 횟수를 제한한다.
- 확정사항 D-022와 동일한 기준으로 적용한다.

```text
답변:
- 프로토타입 1 적용 여부: 적용
- 적용 스테이지: 2·3스테이지
- 발동 확률과 시점: 같은 진행에서 미발동 상태일 때 스테이지 입장 시 한 번, 20%
- 스테이지 전체 아이템 총사용 한도: 1개 또는 2개 중 무작위 선정
- 사용 횟수 집계: 해당 스테이지의 모든 Showdown에서 누적
- 한도 소진 처리: 스테이지 종료 전까지 추가 아이템 사용 불가
- 재등장 처리: 한 진행에서 한 번 발동하면 이후 스테이지 미추첨·미발동
- 2스테이지 미발동 처리: 3스테이지 입장 시 다시 20% 추첨
```

## Q-021. 상점 리롤과 전환 연출 건너뛰기

- 상태: 확정
- 현재 0.1.2.4 기준: 리롤 비용은 1이고 상점 방문당 최대 2회다. 스테이지 전환은 실제 연출 완료까지 입력을 잠그며 건너뛰기를 허용하지 않는다.
- 질문: 리롤 비용·횟수 제한과 전환 연출 건너뛰기 허용 여부를 정한다.

```text
답변:
- 리롤 비용: 리롤 비용 1
- 상점 방문당 리롤 횟수 제한: 2회
- 전환 연출 건너뛰기 허용 여부: 연출 건너뛰기 미허용
- 건너뛰기 입력과 허용 시점:연출 건너뛰기 미허용으로 미구현
```

## Q-022. 플레이 구간 상태 인지와 대체 명칭

- 상태: 확정
- 현재 0.1.2.4 기준: 일반 플레이 화면에는 `할리갈리`, `포커` 문자열을 노출하지 않고 각각 `Three Call`, `Showdown` 명칭과 아이콘·카메라 변화로 구간을 전달한다.
- 질문 1: 플레이어가 현재 카드 획득 구간인지 패 판정 구간인지 무엇으로 구분하는가?
- 질문 2: 두 구간에 인게임 명칭을 표시할지, 아이콘·카메라·배치만으로 구분할지 정한다.
- 영향: 첫 플레이 이해도, 텍스트 최소화, 현지화 범위

### 명칭 후보

| 내부 단계 | 후보 A | 후보 B | 후보 C | 무문자안 |
|---|---|---|---|---|
| 할리갈리 단계 | 카드 쟁탈 | 획득 단계 | 선점 승부 | 벨·카드 아이콘과 클로즈업만 사용 |
| 포커 단계 | 패 대결 | 판정 단계 | 결전 | 카드 팬·공용 카드·와이드 화면만 사용 |

### 세부 상태 후보

- 카드 펼치기: `탐색` 또는 펼치기 아이콘
- 개인 카드 선택: `선택` 또는 `3/3` 수량 표시
- 아이템 사용: `정비` 또는 아이템 슬롯 강조
- 패 확정: `확정` 또는 봉인·자물쇠 아이콘
- 승패 예측: `예측` 또는 플레이어·AI 초상 선택
- 카드 공개·족보 비교: `공개`, `판정` 또는 중앙 비교 연출

```text
답변:
- 할리갈리 단계의 인게임 명칭: Three Call
- 포커 단계의 인게임 명칭: Showdown
- 단계 명칭 텍스트 노출 여부: 아이콘, 라벨 메시지 노출 "'돌입" 로 표현.
- 현재 상태를 알리는 수단(텍스트/아이콘/카메라/배치/복합): 텍스트 제외 전체
- 세부 상태별 표시 방식: 세부 상태 아이콘, 카메라
- 내부 문서·코드 용어는 기존 명칭 유지 여부: 내부 코드 용어는 기존 명칭 유지, 신규 인게임 명칭은 확정 후 공통 사용
- 카드·벨 구간의 신규 인게임 명칭: `Three Call`
- 5장 패 판정 구간의 신규 인게임 명칭: `Showdown`
```


---

# 답변 완료 기록

## 답변 작성 방법

각 항목의 `답변:` 뒤에 내용을 작성한다. 선택지만으로 부족하면 자유롭게 규칙을 추가해도 된다.

답변 예시:

```text
- 답변: 1+1+1은 정답으로 인정하지 않음. 한쪽 카드 더미의 최대 2장만 합산.
```

## Q-001. 해골 1+1+1 인정 여부

- 상태: 확정 → D-001
- 질문: 같은 문양의 해골 1 카드 세 장이 노출된 경우 합계 3의 정답으로 인정하는가?
- 영향: 판정 알고리즘, 정답 빈도, 획득 카드 수
- 결정 반영 위치: D-001, 카드 획득 표, QA 판정 사례
- 답변
```
- 카드 뭉치는 항상 2장이 노출됨
카드 펼치기를 통해서 좌, 우에 카드가 있을 때

왼쪽 펼쳐진 카드
- 하트/해골1
- 하트/해골1

오른쪽 펼쳐진 카드
- 하트/해골1
- 다이아/해골1

위 방식으로  카드가 펼쳐질 경우
- 왼쪽 카드 펼쳐진 카드는 하트2 해골2 로 계산
- 오른쪽 펼쳐진 카드는 하트1/해골1, 다이아1/해골1

로 계산되어 각각 인정되지 않음 왼쪽,오른쪽 펼쳐진 카드는 서로가 독립된 계산을 해야함


```

## Q-002. 벨 하나가 판정하는 카드 범위

- 상태: 확정 → D-001, D-004
- 질문: 벨은 선택한 한쪽 카드 더미의 최대 2장만 판정하는가, 아니면 화면에 공개된 같은 문양 카드 전체를 판정하는가?
- 현재 0.1.0 기준: 선택한 왼쪽 또는 오른쪽 카드 더미의 최대 2장
- 결정 반영 위치: D-001, D-004, 필드 UI

- 답변
```
벨은 2개의 선택지, 좌,우 벨이 주어진 만큼 각각 좌, 우 의 벨만 판정함
화면에 공개된 같은 문양을 판단하지 않음
```

## Q-003. 공개 진행 중 카드의 판정 시작 이벤트

- 상태: 확정 → D-005
- 질문: 카드가 어느 상태에 들어오면 벨 판정 대상으로 인정하는가?
- 선택 참고: `CardRevealStart`부터 인정 / 앞면 전환 완료부터 인정 / `CardRevealComplete`부터 인정
- 영향: 플레이어가 카드 정보를 보기 전에 AI 또는 입력이 판정되는 문제, 구현 이벤트 순서
- 결정 반영 위치: D-005, 입력 잠금 상태표

- 답변
```
- 카드는 앞면 전환 완료부터 인정
```

## Q-004. 미선택 카드 더미의 최상단 카드 이동 목적지

- 상태: 확정 → D-005
- 질문: 공개 중 벨 판정이 끝났을 때 미선택 카드 더미의 최상단 카드는 어디로 이동하는가?
- 선택 참고: 미획득 카드풀 / 해당 더미 맨 아래 / 현재 자리에 유지 / 기타
- 결정 반영 위치: D-005, 저장소 전이표

- 답변
```
- 미선택 카드 더미의 최상단 카드는 미획득 해당 더미 맨 아래로 이동
```
## Q-005. 공개 도중 벨 결과가 확정된 뒤 남은 배분

- 상태: 확정 → D-006
- 질문: 4장 순차 공개 중 두 번째 또는 세 번째 카드에서 벨 결과가 확정되면 남은 카드를 계속 공개하는가, 즉시 배분을 중단하고 다음 할리갈리 라운드로 넘어가는가?
- 영향: 카드 소비량, 공개 순서, AI 반응과 연출
- 결정 반영 위치: D-006, 카드 소비 규칙, 상태 전이

- 답변
```
- 남은 카드의 배분을 중단, 다음 라운드로 넘어감.
```

## Q-006. 재셔플 시작 조건과 재셔플 후에도 카드가 부족한 경우

- 상태: 확정 → D-008, D-015
- 질문 1: 카드 펼치기 저장소가 4장 미만일 때 재셔플하는가, 다음 한 장이 필요할 때마다 재셔플하는가?
- 질문 2: 미획득 카드풀을 합쳐도 필요한 수량이 부족하면 할리갈리 단계를 종료하는가?
- 질문 3: 현재 필드에 노출됐지만 획득되지 않은 카드도 재셔플에 포함하는가?
- 결정 반영 위치: D-008, 단계 종료 조건, 시드 규칙


- 답변
```
- 저장소가 4장 미만이라도 일단 할리갈리 단계를 진행 > 카드가 없을 경우 할리갈리 단계 종료, 플레이어에게 3장이 되기 위해 카드 랜덤 지급

- 재셔플은 포커 라운드가 끝난 후 새 시드를 통한 게임 지급 외에 없음



```

## Q-007. 개인 획득 카드가 3장을 초과한 경우

- 상태: 확정 → D-007
- 질문: 개인 저장 카드가 4장 이상이면 포커에 사용할 3장을 직접 선택하는가?
- 현재 0.1.0 기준: 전투 라운드별로 승자가 3장 또는 2장을 직접 확정하고 나머지를 무작위 보충
- 추가 질문: 기존 `3 → 2 → 2` 직접 선택 규칙을 유지하는가?
- 답변:
- 결정 반영 위치: D-007, 개인 카드 선택 UI
```
- 개인 카드 3장 초과 시 포커에 사용할 3장은 직접 선택함
- 기존 3 > 2 > 2 직접 선택 규칙 유지
```

## Q-008. 개인 획득 카드가 3장 미만인 경우

- 상태: 확정 → D-007, D-015
- 질문: 부족한 개인 카드는 어느 카드풀에서 어떤 순서로 보충하는가?
- 현재 0.1.0 기준: 랜덤 획득 후보군에서 시드 기반 무작위 보충
- 답변:
- 결정 반영 위치: D-007, 무작위 보충 규칙
```
- 할리갈리 단계 종료 시점에서 부족한 개인 카드는 미획득 카드풀에서 랜덤으로 지급
- 카드 펼치기를 위해 사용하려 했던 카드들 또한 할리갈리 단계 종료 시 미획득 카드풀로 이동되어 랜덤 지급에 포함되도록 설정
```

## Q-009. 할리갈리 단계 패자의 획득 카드 유지 여부

- 상태: 확정 → D-007
- 질문: 단계 패자도 자신이 할리갈리 중 획득한 카드를 개인 저장 카드로 유지하는가?
- 현재 0.1.0 기준: 패자의 획득 카드는 랜덤 후보군에 반환하고 개인 카드 3장을 무작위 획득
- 답변:
- 결정 반영 위치: D-007, 단계 종료 배분
```
- 단계 패자또한 올바른 벨을 선택해 카드를 획득하는 경우 해당 카드는 획득 가능 및 개인 저장 카드로 유지
- 3 > 2 > 2 인 만큼 패자 또한 3장 초과의 카드를 획득하는 경우가 생길 수 있음, 이 상태는 패자 또한 카드 선택을 할수 있게 제공
```

## Q-010. 개인 획득 카드 저장소의 유지 기간

- 상태: 확정 → D-007
- 질문: 개인 획득 카드는 한 번의 전투 라운드가 끝나면 초기화하는가, HP가 0이 될 때까지 다음 전투 라운드에도 유지하는가?
- 현재 0.1.0 기준: 전투 라운드마다 새 52장 덱과 새 개인 카드 구성
- 답변:
- 결정 반영 위치: D-007, 전투 라운드 초기화
```
- 0.1.0 기준 규칙, 전투 라운드마다 새 52장 덱과 개인카드 구성 규칙 유지
```

## Q-011. 아이템 사용 가능 시점과 재예측 반복

- 상태: 확정 → D-013, D-014
- 질문 1: 아이템은 최초 승패 예측 전에만 사용할 수 있는가, 예측 후에도 사용할 수 있는가?
- 질문 2: 카드 변경 아이템을 여러 번 사용할 수 있다면 변경될 때마다 예측을 다시 진행하는가?
- 현재 0.1.0 기준: 아이템 사용 → 패 확정 → 승패 예측
- 답변:
- 결정 반영 위치: D-014, 포커 단계 상태 전이
```
- 아이템은 승패 예측 전에만 사용한다.
- 아이템 사용과 카드 변경을 모두 마친 최종 카드 상태에서 승패 예측을 한 번 진행한다.
- 기존 카드 변경 시 재예측 규칙은 폐기한다.
```

## Q-012. 카드 변경 후 재예측 제한 시간

- 상태: 확정 → D-013, D-014
- 질문: 아이템으로 카드가 변경되어 예측을 다시 받을 때 제한 시간을 처음부터 다시 시작하는가, 기존 남은 시간을 이어 사용하는가?
- 현재 승패 예측 제한 시간: 1분
- 결정 반영 위치: D-014, 제한 시간 상태표

- 답변
```
- 승패 예측 제한 시간을 1분에서 2분으로 변경한다.
- 카드 변경으로 예측 시간을 초기화하거나 추가하지 않고 동일한 2분 제한을 유지한다.
```

## Q-013. 30초 제한 시간의 적용 상태

- 상태: 확정 → D-005, D-009
- 질문: 30초 제한은 다음 카드 펼치기 입력 대기 시간에만 적용하는가, 정답 조건이 생긴 뒤 벨 입력에도 적용하는가?
- 현재 0.1.0 기준: 플레이어가 시작 참가자인 카드 펼치기 입력 대기에 30초 적용, 벨 입력에는 별도 제한 없음
- 결정 반영 위치: D-009, 제한 시간 상태표

- 답변
```
- 30초 제한은 벨 입력에 적용한다.
- 승패가 난 직후 다음 진행에 2초 펼치기 잠금을 1회만 적용한다.
- 2초 잠금이 끝나면 30초 제한 타이머를 시작하며, 이후에는 공개 중에도 앞면 전환을 완료한 카드를 대상으로 벨 입력할 수 있다.
```

## Q-014. 할리갈리 최대 진행 횟수

- 상태: 확정 → D-015
- 질문: 재셔플 규칙을 도입한 뒤에도 최대 12회 4장 배분 제한을 유지하는가?
- 참고: 이전 문서에는 26회·25회·12회 기준이 혼재했으며 최신 0.1.0은 최대 12회 4장 배분이다.
- 추가 질문: 한도 안에 승리 목표를 달성하지 못하면 기존 무승자 개인 카드 배분 규칙을 유지하는가?
- 답변:
- 결정 반영 위치: 단계 종료 조건, QA 수용 기준
```
- 최대 12회, 1회당 최대 4장 배분 제한을 유지한다.
- 한도 안에 있더라도 펼치기에 사용할 카드가 없으면 할리갈리 단계를 종료한다.
- 종료 시 개인 카드가 3장 미만이면 미획득 카드풀에서 무작위로 지급해 정확히 3장을 구성한다.
```


---

# 프로그래머 전달 전 필수 확정 묶음

Q-001~Q-014는 모두 확정되었다. 프로그래머 전달 시 다음 구현 기준을 묶어서 전달한다.

1. D-001~D-006: 좌우 독립 판정, 앞면 전환 완료 카드만 판정, 승부 확정 시 남은 공개 중단
2. D-007~D-008: 양측 획득 카드 유지, 3장 직접 선택·무작위 보충, 전투 중 재셔플 금지
3. D-009: 승패 직후 2초 잠금 1회, 이후 30초 벨 입력 제한
4. D-013~D-014: 아이템 선사용, 재예측 폐기, 최종 카드로 2분 예측 1회
5. D-015: 최대 12회 4장 배분, 카드 소진 시 조기 종료와 3장 보충
6. D-007·D-009: 오답·시간 초과 시 승리 1회만 기록하고 양측의 기존 획득 카드는 유지
7. D-016: 기본·일회성 재화를 분리하며 정확한 지급량은 Q-015~Q-016 답변 확정 전까지 데이터 미정값으로 유지
8. D-017: 최초 시작과 다음 스테이지 모두 살룬 입장·중앙 원형 테이블 착석 뒤 전투 시작

---

# 결정 이력

| 날짜 | ID | 변경 내용 | 상태 |
|---|---|---|---|
| 2026-08-08 | D-001~D-004 | 같은 문양 해골 합계 3, 1·2 동시 획득, 해골 3 단독 획득, 먼저 누른 쪽 정답 | 확정 |
| 2026-08-08 | D-005~D-006 | 승패 직후 2초 잠금 이후 공개 중 벨 판정, 앞면 전환 완료 기준, 승부 시 남은 공개 중단 | 확정 |
| 2026-08-08 | D-007~D-008 | 양측 획득 카드 유지, 3장 선택·보충, 전투 중 재셔플 금지와 카드 소진 종료 | 확정 |
| 2026-08-08 | D-011~D-014 | 정확히 5장 포커, 최신 동률 규칙, 아이템 선사용, 재예측 폐기, 2분 예측 | 확정 |
| 2026-08-08 | D-015 | 최대 12회 4장 배분 유지, 카드 소진 시 조기 종료·3장 미달 무작위 보충 | 확정 |
| 2026-08-08 | D-007·D-009 | 오답·시간 초과 시 상대 승리 1회만 기록, 기존 획득 카드 유지, 별도 보상 카드 선택 없음 | 확정 |
| 2026-08-08 | D-016 | AI HP 0 도달 시 잔여 플레이어 HP 1당 총알 1개 정산 | 0.1.2.3에서 지급량 재검토로 대체 |
| 2026-08-12 | Q-015~Q-017·Q-019·Q-021 | 재화 지급표·회복·최종 분기·리롤과 건너뛰기 답변 반영 | 확정 |
| 2026-08-12 | Q-018·Q-022 | 패 확정 시간 초과 처리와 `Three Call`·`Showdown` 인게임 명칭 확정 | 확정 |
| 2026-08-12 | Q-020·D-022 | 2·3스테이지 20% 추첨, 스테이지 총사용 한도 1~2개 무작위, 누적 사용·재등장 금지 | 확정 |
| 2026-08-08 | D-017 | 최초 시작과 다음 스테이지에 살룬 스윙도어 입장·중앙 원형 테이블 착석 연출 적용 | 확정 |
| 2026-08-08 | Q-001~Q-014 | 사용자 답변을 D-001~D-015에 반영하고 미확정 항목 해소 | 확정 |
